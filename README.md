所有模型操作都是CUDA_VISIBLE_DEVICES=0,1,2,3 bash scripts/train_larp_tokenizer_reproduce.sh
需要去train_larp_tokenizer_reproduce.sh文件里面修改一下config的yaml，
即--cfg cfgs/larp_tokenizer_stat.yaml，这些cfg都在cfg文件夹里
然后改一下outpath，csv和batchsize
如果bs较大的话大概50-100个epoch左右就可以，baseline那个模型建议跑100个看看最终效果
